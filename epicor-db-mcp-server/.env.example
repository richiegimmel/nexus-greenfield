# Epicor SQL Server Read-Only Database Connection
# IMPORTANT: If your password contains # or @ characters, you MUST wrap it
# in double quotes or dotenv will silently truncate it at the # (comment char).
SQL_SERVER_HOST=erpus-read08.epicorsaas.com
SQL_SERVER_DATABASE=SaaS5034_160144
SQL_SERVER_USER=C160144RO
SQL_SERVER_PASSWORD="your-password-here"
SQL_SERVER_PORT=50127

# Optional: CSV file paths for offline table/relationship metadata
# (If not set, the CSV-based tools will be unavailable but DB query tools still work)
# EPICOR_CSV_PATH=/path/to/Ice.TableAttribute.csv
# EPICOR_RELATIONS_CSV_PATH=/path/to/Ice.QueryRelation.csv
# EPICOR_RELATION_FIELDS_CSV_PATH=/path/to/Ice.QueryRelationField.csv
